'''

    This is just a directory for the models.admin module.

    Administrative models are objects that we save in the database that subclass
    models.Model but aren't createable or editable by end-users.

'''

from app.models.admin._class_methods import *
